import React, { useEffect, useState } from 'react';
import { Member } from '@/types';

const addExpense = () => {

    const [individualExpense, setIndividualExpense] = useState<number>();
    const [members, setMembers] = useState<Member[]>([])

    return (
        <div>
            
        </div>
    );
}

export default addExpense;
